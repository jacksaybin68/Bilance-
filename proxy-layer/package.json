{
  "name": "okbong-proxy-layer",
  "version": "1.0.0",
  "private": true,
  "description": "Dev reverse proxy — gom user frontend (:3001), admin frontend (:5173) và backend API (:3000) về một port duy nhất (:8080).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "http-proxy": "^1.18.1"
  }
}
